name: otakoyi_test

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter_svg: 0.18.0
  intl: ^0.16.1
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
    - fonts/

  fonts:
    - family: WorkSans
      fonts:
        - asset: fonts/Worksans.ttf

  uses-material-design: true
